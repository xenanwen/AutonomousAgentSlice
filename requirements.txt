# Ranges rather than exact pins, so pip can pick a build that has a prebuilt
# wheel for whatever Python version you are on. Exact pins from an older date
# force pip to compile pydantic-core from source on newer Pythons, which needs
# a Rust toolchain and usually fails.
fastapi>=0.110,<1.0
uvicorn>=0.29,<1.0
pydantic>=2.7,<3.0
SQLAlchemy>=2.0,<2.1

# testing
pytest>=8.0,<9.0
httpx>=0.27,<1.0
